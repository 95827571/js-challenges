let favHolidayDestinations = [
    "Venice, Italy",
    "Paris, France",
    "Barcelona, Spain",
    "Majorca, Spain",
    "Lapland, Finnish"
]

for (let i = 0; i < favHolidayDestinations.length; i++) {
    const element = favHolidayDestinations[i];
    console.log(element);
}